<Window x:Class="Ideo.TopSolidLauncher.Views.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Saisie" Width="520" Height="255" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Style="{StaticResource SectionCard}" Margin="24,22">
            <StackPanel>
                <TextBlock x:Name="PromptText" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />
                <TextBox x:Name="ValueTextBox" Margin="0,14,0,0" />
                <TextBlock x:Name="FormMessageText" Foreground="{StaticResource WarningBrush}" Margin="0,8,0,0" />
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Background="{StaticResource FooterBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0" Padding="20,13">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Annuler" Style="{StaticResource SecondaryButton}" IsCancel="True" Margin="0,0,10,0" />
                <Button Content="Valider" IsDefault="True" Click="Confirm_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
